# 快速入门

创建 Maven 工程，不用选择 archetype：在 pom.xml 中添加 SpringBoot 的起步依赖：

![](assets/document_image_rId4.png)

![](assets/document_image_rId4.png)

所有 SpringBoot 工程都必须继承 spring-boot-starter-parent。

<parent>  <groupId>org.springframework.boot</groupId>  <artifactId>spring-boot-starter-parent</artifactId>  <version>2.3.12.RELEASE</version></parent>

要集成 SpringMVC 需要导入如下依赖：

<dependency>  <groupId>org.springframework.boot</groupId>  <artifactId>spring-boot-starter-web</artifactId></dependency>

编写 SpringBoot 引导类：

@SpringBootApplicationpublic class MySpringBootApplication {    public static void main(String[] args) {        SpringApplication.run(MySpringBootApplication.class);    }}

编写 Controller：

在引导类 MySpringBootApplication 同级或者子级包中创建。

@Controllerpublic class QuickStartController {    @RequestMapping("/quick")    @ResponseBody    public String quick() {        return "SpringBoot Hello";    }}

此时的项目结构是这样的：

![](assets/document_image_rId4.png)

运行引导类主方法，控制台打印类似日志说明成功启动：

打开浏览器输入地址 http://localhost:8080/quick。

![](assets/document_image_rId4.png)

![](assets/document_image_rId4.png)

@SpringBootApplication：声明该类是一个 SpringBoot 引导类。

SpringApplication.run(MySpringBootApplication.class)：run 方法表示运行 SpringBoot 的引导类，参数是引导类的字节码对象。