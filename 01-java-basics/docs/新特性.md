# 新特性

## 可变参数

可变参数是 JDK 1.5 之后出现的新特性。当方法的参数列表数据类型已经确定，但是参数的个数不确定，就可以使用可变参数。可变参数底层就是一个数组，根据传递参数个数不同，会创建不同长度的数组，来存储这些参数。

注意：

如果在方法书写时，这个方法拥有多参数，参数中包含可变参数，可变参数一定要写在参数列表的末尾位置。

一个方法的参数列表，只能有一个可变参数。

public static void method(Object... obj){}

其中 Object 可以换做任意具体类型，当参数类型为 Object 时，可以传递任意类型。