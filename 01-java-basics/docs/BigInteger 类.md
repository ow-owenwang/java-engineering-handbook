# BigInteger 类

Integer 类作为 int 的包装类，能存储的最大整型值为2^31−1，BigInteger 类的数字范围较 Integer 类的数字范围要大得多，可以支持任意精度的整数。

# 构造器

BigInteger(String val)

# 常用方法

方法

功能描述

abs()

add(BigInteger val)

subtract(BigInteger val)

multiply(BigInteger val)

divide(BigInteger val)

remainder(BigInteger val)

pow(int exponent)

divideAndRemainder(BigInteger val)