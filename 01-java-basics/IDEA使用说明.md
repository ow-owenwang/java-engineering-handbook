# IDEA 使用说明

## 问题：打开 Maven 项目后没有执行按钮

如果使用 IDEA 打开项目后，Java 文件没有执行按钮（运行按钮），请按照以下步骤操作：

### 方法一：正确导入 Maven 项目（推荐）

1. **打开项目**：
   - 在 IDEA 中选择 `File` -> `Open`
   - 选择具体的项目目录，例如：`01-java-basics/array/code/array-demo`
   - **重要**：不要打开整个 `01-java-basics` 目录，而是打开每个知识点下的 `code/xxx-demo` 目录

2. **导入 Maven 项目**：
   - IDEA 会自动检测到 `pom.xml` 文件
   - 如果出现提示，选择 "Import Maven Project" 或 "Open as Project"
   - 等待 Maven 依赖下载完成（如果有依赖）

3. **标记源代码目录**（如果需要）：
   - 右键点击 `src/main/java` 目录
   - 选择 `Mark Directory as` -> `Sources Root`
   - 如果存在 `src/test/java`，标记为 `Test Sources Root`

4. **刷新 Maven 项目**：
   - 点击右侧 Maven 工具窗口
   - 点击刷新按钮（🔄）或使用快捷键 `Ctrl+Shift+O` (Windows/Linux) / `Cmd+Shift+I` (Mac)

### 方法二：如果已经打开了项目

1. **重新导入 Maven 项目**：
   - 右键点击项目根目录
   - 选择 `Maven` -> `Reload Project`

2. **检查项目结构**：
   - `File` -> `Project Structure` (快捷键：`Ctrl+Alt+Shift+S` / `Cmd+;`)
   - 在 `Modules` 中检查：
     - `src/main/java` 应该标记为 `Sources`
     - `src/test/java` 应该标记为 `Tests`（如果存在）

3. **检查 SDK 设置**：
   - 在 `Project Structure` -> `Project` 中
   - 确保 `Project SDK` 已设置（Java 8 或更高版本）
   - 确保 `Project language level` 与 `pom.xml` 中的配置一致

### 验证项目是否正确配置

打开一个包含 `main` 方法的 Java 文件，例如：
- `array/code/array-demo/src/main/java/org/example/Demo03.java`

如果配置正确，你应该能看到：
- 类名旁边有绿色的运行按钮（▶️）
- 或者在 `main` 方法左侧有运行按钮

### 常见问题

1. **没有运行按钮**：
   - 确保打开的是 `code/xxx-demo` 目录，而不是整个知识点目录
   - 确保 `src/main/java` 被标记为 Sources Root
   - 尝试重新导入 Maven 项目

2. **编译错误**：
   - 检查 JDK 版本是否匹配（项目使用 Java 8）
   - 在 `File` -> `Project Structure` -> `Project` 中设置正确的 SDK

3. **Maven 依赖问题**：
   - 检查网络连接
   - 尝试 `Maven` -> `Reload Project`
   - 或者在终端运行 `mvn clean install`

### 项目结构说明

每个知识点的项目结构如下：
```
知识点名/
├── README.md              # 知识点说明文档
└── code/
    └── 知识点名-demo/      # Maven 项目（在 IDEA 中打开这个目录）
        ├── pom.xml        # Maven 配置文件
        └── src/
            ├── main/
            │   └── java/  # 源代码目录（Sources Root）
            └── test/
                └── java/  # 测试代码目录（Test Sources Root，如果存在）
```

### 快速打开项目

在 IDEA 中：
1. `File` -> `Open`
2. 导航到：`01-java-basics/知识点名/code/知识点名-demo`
3. 点击 `OK`

例如：
- `01-java-basics/array/code/array-demo`
- `01-java-basics/collection/code/collection-demo`
- `01-java-basics/oop/code/oop-demo`
